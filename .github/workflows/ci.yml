name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test-action-ubuntu-latest:
    uses: ./.github/workflows/test-action.yml
    with:
      runs-on: ubuntu-latest

  test-action-windows-latest:
    uses: ./.github/workflows/test-action.yml
    with:
      runs-on: windows-latest

  test-action-macos-latest:
    uses: ./.github/workflows/test-action.yml
    with:
      runs-on: macos-latest
